<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="#F5F5F5"
    tools:context=".ui.admin.BuddhaAssignF">

    <!-- Search Box -->
    <LinearLayout
        android:background="#FFFFFF"
        android:padding="16dp"
        android:elevation="4dp"
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">

        <TextView
            android:id="@+id/txt_search_title"
            android:text="ค้นหาลูกค้าเพื่อถวายพระพุทธรูป"
            android:textSize="16sp"
            android:textStyle="bold"
            android:textColor="#333333"
            android:layout_marginBottom="8dp"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"/>

        <LinearLayout
            android:orientation="horizontal"
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <EditText
                android:id="@+id/edt_find_user"
                android:layout_weight="1"
                android:background="@android:drawable/edit_text"
                android:hint="ชื่อ, Username หรือ User ID"
                android:textSize="16sp"
                android:padding="12dp"
                android:layout_marginEnd="8dp"
                android:layout_width="0dp"
                android:layout_height="wrap_content" />

            <Button
                android:id="@+id/btn_find_user"
                android:text="ค้นหา"
                android:textColor="#FFFFFF"
                android:background="#4CAF50"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content" />

        </LinearLayout>

    </LinearLayout>

    <!-- Status Text -->
    <TextView
        android:id="@+id/txt_search_status"
        android:text="รายชื่อลูกค้าล่าสุด"
        android:textSize="14sp"
        android:textStyle="bold"
        android:textColor="#666666"
        android:padding="16dp"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>

    <!-- Progress Bar -->
    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:visibility="gone"/>

    <!-- User List -->
    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycle_user"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:padding="8dp"/>

</LinearLayout>
